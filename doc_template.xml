<?xml version="1.0"?>
<toolset_description># Insomnia

Insomnia is a collection of modding tools and research for Insomniacs 7th gen engine for PS3 platform

This toolset runs on Spike foundation.

Head to this **[Wiki](https://github.com/PredatorCZ/Spike/wiki/Spike)** for more information on how to effectively use it.
</toolset_description>

<region_to_gltf name="Region to GLTF">Converts entire region map to GLTF.</region_to_gltf>

<extract_sound name="Extract sound">Extracts sound files and converts them into WAV format.</extract_sound>

<extract_assets name="Extract Assets">Extracts packed assets, converts textures.
In addition, converts mobys and ties into GLTF format.
> [!IMPORTANT]
> Moby animations don't have scale tracks for now, because they use aligned inheritance, that GLTF doesn't support.
> Some moby animations for large objects might have biased tranlations, cause is uknown.</extract_assets>

<extract_effect name="Extract Effect">Extracts and converts textures from `vfx_system_header.dat`.</extract_effect>

<extract_psarc name="Extract PSARC">Extracts psarc archives.</extract_psarc>

<levelmain_to_gltf name="Levelmain to GLTF">Converts level from Resistance: Fall of Man to GLTF.
Currently there are some missing features:

- detail textures
- scene lighting data (per vertex lightmaps, texel directional lightmaps)
- moby instances (individual mobys are converted to GLTF)
- Water(?)
- Skybox

Textures are embedded in the GLTF file and converted to be up to GLTF standard.
Textures, that were unable to be linked to GLTFs are normally extracted, however unconverted (normal and template maps)

> [!IMPORTANT]
> Moby animations don't have scale tracks for now, because they use aligned inheritance, that GLTF doesn't support.
> Some moby animations for large objects might have biased tranlations, cause is uknown.</levelmain_to_gltf>

<loc_to_json name="Localization to JSON">Converts localization package from Resistance: Fall of Man to JSON.</loc_to_json>

<extract_textures name="Extract texture banks">Extracts textures from tp/tph texture banks.</extract_textures>

<toolset_footer>## [Latest Release](https://github.com/PredatorCZ/InsomniaToolset/releases)

## License

This toolset is available under GPL v3 license. (See LICENSE)\
This toolset uses following libraries:

- Spike, Copyright (c) 2016-2025 Lukas Cone (Apache 2)
</toolset_footer>
